import 'package:flutter/material.dart';

class WarningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Warning')),
      body: Center(child: Text('Warning Screen')),
    );
  }
}
